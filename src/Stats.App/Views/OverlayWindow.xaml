<Window x:Class="Stats.App.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Stats.App.Controls"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Grid>
        <Border Background="#E01B1B1C" CornerRadius="8" Padding="12,8">
            <Border.LayoutTransform>
                <ScaleTransform ScaleX="{Binding FontScale}" ScaleY="{Binding FontScale}"/>
            </Border.LayoutTransform>
            <StackPanel>
                <Canvas Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}" Visibility="{Binding HasCustomCanvas, Converter={StaticResource BoolToVis}}">
                    <ItemsControl ItemsSource="{Binding CanvasTiles}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemContainerStyle><Style TargetType="ContentPresenter"><Setter Property="Canvas.Left" Value="{Binding X}"/><Setter Property="Canvas.Top" Value="{Binding Y}"/></Style></ItemsControl.ItemContainerStyle><ItemsControl.ItemTemplate><DataTemplate><Border Width="{Binding Width}" Height="{Binding Height}" CornerRadius="4" Padding="10"><Border.Style><Style TargetType="Border"><Setter Property="BorderThickness" Value="0"/><Setter Property="BorderBrush" Value="#FF00E5FF"/><Style.Triggers><DataTrigger Binding="{Binding DataContext.HasFixedNeonBorder, RelativeSource={RelativeSource AncestorType=Window}}" Value="True"><Setter Property="BorderThickness" Value="2"/></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel><TextBlock Text="{Binding Tile.DisplayName}" FontSize="10" Foreground="{StaticResource OverlayTextSecondary}"/><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding Tile.CurrentText}" FontSize="16" FontWeight="SemiBold" Foreground="{Binding Tile.Severity, Converter={StaticResource SeverityToBrush}, ConverterParameter=Overlay}"/><controls:Sparkline Width="56" Height="16" Margin="6,0,0,0" Values="{Binding Tile.HistoryValues}" Capacity="{Binding Tile.HistorySampleCapacity}" Stroke="{Binding Tile.Severity, Converter={StaticResource SeverityToBrush}, ConverterParameter=Overlay}" ShowGuides="False" Visibility="{Binding DataContext.ShowSparklines, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVis}}"/></StackPanel></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                </Canvas>
                <ItemsControl x:Name="TilesHost" ItemsSource="{Binding Tiles}" HorizontalAlignment="Left" Visibility="{Binding HasAutomaticCanvas, Converter={StaticResource BoolToVis}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="{Binding DataContext.Orientation, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource OverlayOrientation}}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="10,2">
                                <TextBlock Text="{Binding DisplayName}" FontSize="10"
                                           Foreground="{StaticResource OverlayTextSecondary}"
                                           TextTrimming="CharacterEllipsis" MaxWidth="140"/>
                                <StackPanel Orientation="{Binding DataContext.Orientation, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource OverlayOrientation}}">
                                    <TextBlock x:Name="ValueText" Text="{Binding CurrentText}" FontSize="16" FontWeight="SemiBold" Typography.NumeralAlignment="Tabular"
                                               Foreground="{Binding Severity, Converter={StaticResource SeverityToBrush}, ConverterParameter=Overlay}"/>
                                    <controls:Sparkline Width="56" Height="{Binding ActualHeight, ElementName=ValueText}"
                                                         Values="{Binding HistoryValues}" Capacity="{Binding HistorySampleCapacity}"
                                                         Stroke="{Binding Severity, Converter={StaticResource SeverityToBrush}, ConverterParameter=Overlay}"
                                                         ShowGuides="False" IsHitTestVisible="False"
                                                         Visibility="{Binding DataContext.ShowSparklines, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVis}}">
                                        <controls:Sparkline.Style>
                                            <Style TargetType="controls:Sparkline">
                                                <Setter Property="Margin" Value="6,0,0,0"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.Orientation, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="Vertical">
                                                        <Setter Property="Margin" Value="0,2,0,0"/>
                                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </controls:Sparkline.Style>
                                    </controls:Sparkline>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="{Binding FrameStatusText}"
                           FontSize="11" MaxWidth="260" TextWrapping="Wrap" Margin="10,0">
                    <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource OverlayTextSecondary}"/><Style.Triggers>
                        <DataTrigger Binding="{Binding FrameStatusIsWarning}" Value="True"><Setter Property="Foreground" Value="{StaticResource OverlayWarn}"/></DataTrigger>
                        <Trigger Property="Text" Value=""><Setter Property="Visibility" Value="Collapsed"/></Trigger>
                    </Style.Triggers></Style></TextBlock.Style>
                </TextBlock>
                <!-- status strip: bottom edge in both orientations, inside the Border so FontScale and the
                     move-mode outline cover it -->
                <!-- MaxWidth and the 10/4 inset live on one Border, as Padding, so the strip's whole footprint is
                     bounded by the tiles' width, and TilesHost is left-aligned so its ActualWidth is the tiles'
                     natural width rather than the StackPanel's: otherwise the strip's own width (a Margin outside
                     MaxWidth, or one unbreakable word) widens the StackPanel, which widens TilesHost, which raises
                     MaxWidth again — a feedback loop that grew the panel until the text fit instead of wrapping.
                     ClipToBounds keeps a word wider than the tiles from painting past the strip. -->
                <Border Padding="10,4,10,0" ClipToBounds="True"
                        HorizontalAlignment="Left"
                        Visibility="{Binding HasStatus, Converter={StaticResource BoolToVis}}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="MaxWidth" Value="{Binding ActualWidth, ElementName=TilesHost}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasItems, ElementName=TilesHost}" Value="False">
                                    <Setter Property="MaxWidth" Value="240"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Path Style="{StaticResource IconGlyph}" Data="{StaticResource Icon.Warn}" Width="12" Height="12"
                          Fill="{StaticResource OverlayWarn}" Margin="0,1,4,0" VerticalAlignment="Top"
                          Visibility="{Binding StatusIsWarning, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Grid.Column="1" Text="{Binding StatusText}" FontSize="10" TextWrapping="Wrap" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource OverlayTextSecondary}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusIsWarning}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource OverlayWarn}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                </Border>
            </StackPanel>
        </Border>
        <!-- Move-mode outline: dashed AccentBrush rectangle over the panel bounds, click-through so it never
             steals the drag/click handling above. -->
        <Rectangle IsHitTestVisible="False" RadiusX="8" RadiusY="8"
                   Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" StrokeDashArray="4,2"
                   Visibility="{Binding IsMoveMode, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</Window>
