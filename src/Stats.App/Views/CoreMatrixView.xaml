<UserControl x:Class="Stats.App.Views.CoreMatrixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource TileBg}" CornerRadius="6" Margin="6" Padding="10" HorizontalAlignment="Left">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Cores — load · clock · temp"
                       Foreground="{StaticResource TextSecondary}" FontSize="11"/>
            <ItemsControl ItemsSource="{Binding Cells}" Margin="0,6,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding Columns}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="56" Height="50" Margin="2" CornerRadius="3"
                                Background="{Binding Load01, Converter={StaticResource HeatToBrush}}">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Index, StringFormat='{}#{0}'}" FontSize="9"
                                           Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding LoadText}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{Binding Load01, Converter={StaticResource HeatToForeground}}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding ClockText}" FontSize="9"
                                           Foreground="{Binding Load01, Converter={StaticResource HeatToForeground}}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding TempText}" FontSize="9"
                                           Foreground="{Binding Severity, Converter={StaticResource SeverityToBrush}}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>
    </Border>
</UserControl>
