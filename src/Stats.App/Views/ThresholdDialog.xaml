<Window x:Class="Stats.App.Views.ThresholdDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thresholds" Width="380" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource FlyoutBg}">
    <StackPanel Margin="16">
        <TextBlock x:Name="MetricNameText" FontSize="{StaticResource FontSizeSection}" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimary}" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                <TextBlock x:Name="WarnLabel" Text="Warn" Foreground="{DynamicResource TextSecondary}" FontSize="{StaticResource FontSizeLabel}" Margin="0,0,0,4"/>
                <TextBox x:Name="WarnBox"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                <TextBlock x:Name="CritLabel" Text="Crit" Foreground="{DynamicResource TextSecondary}" FontSize="{StaticResource FontSizeLabel}" Margin="0,0,0,4"/>
                <TextBox x:Name="CritBox"/>
            </StackPanel>
        </Grid>
        <TextBlock x:Name="DirectionNote" Foreground="{DynamicResource TextSecondary}" FontSize="{StaticResource FontSizeDense}"
                   FontStyle="Italic" TextWrapping="Wrap" Margin="0,6,0,0"/>
        <CheckBox x:Name="LowerIsWorseCheck" Content="Lower is worse" Margin="0,8,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=ErrorText}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <Path Style="{StaticResource IconGlyph}" Data="{StaticResource Icon.Warn}" Width="12" Height="12"
                  Fill="{DynamicResource CritBrush}" VerticalAlignment="Top" Margin="0,1,6,0"/>
            <TextBlock x:Name="ErrorText" Foreground="{DynamicResource CritBrush}" FontSize="{StaticResource FontSizeDense}" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Clear" Click="Clear_Click" Style="{StaticResource HeaderButton}" MinWidth="72"/>
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource HeaderButton}" MinWidth="72" Margin="8,0,0,0"/>
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Style="{StaticResource PrimaryButton}" MinWidth="72" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
