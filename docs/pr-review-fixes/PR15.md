# PR #15 review repairs

Branch: `feature/graph-effects`. Reviewed original head: `fc99512`.
Ported from the independently reviewed integration repair `c42264b`; no other PR feature is merged here.

Invalidate Sparkline and HistoryChart caches on bound buffer changes, including skipped renders and reused mutable buffers.

## Verification

- `dotnet build Stats.sln --nologo -v:q`: passed, 0 warnings, 0 errors.
- `dotnet test Stats.sln --nologo --no-build --no-restore -v:q`: 727 Core + 188 preview tests passed, 0 failed/skipped.
- 1 isolated WPF RenderTargetBitmap captures completed with 0 warnings using
  `dotnet tools/Stats.UiPreview/bin/Debug/net8.0-windows/Stats.UiPreview.dll --batch C:/claude-projects/Stats/artifacts/pr-fixes-branches/captures-15.json`.
- Local images: `C:/claude-projects/Stats/artifacts/pr-fixes-branches/evidence/pr15-*.png`.
- Independent Sol review found no material port/regression issue. Captures use simulated data at 96 DPI.
- Independent Sol visual validation inspected the actual PNGs and passed the scoped scenarios. Dashboard 1180x720 capture canvases include 39 transparent bottom rows; they are not 720px client-area or 150%-DPI checks.
- New test files are included in this commit. WPF test-project enablement retains the System.IO global using; no package changes.

Terra performed the bounded source/test port; the parent owned project files and validation, with independent Sol review/visual validation. Explicit role routing reused the successful session preflight; effective model metadata is unobservable.

Native routed keyboard/menu-close focus, physical drag feel, live theme transitions, actual 150% DPI, real overlay/hotkeys/tray,
PresentMon, and hardware/fan checks remain pending. Tests call rendering/cache helpers directly, not the full native interaction path.

## Merge resolution after PR #14

Merged master `ec3de85` into PR #15 at `db81a38`, retaining both layout and graph settings, compatibility tests,
preview substates, and all 64 unique baseline captures. Existing captures retain explicit RTB mode.
Independent source review found no material resolution or auto-merge regression.

Resolved build: 0 warnings/errors. Tests: 800 Core + 205 preview passed, none failed/skipped.
Five isolated captures cover Free layout at 0.9/1.3 scale, detail warmup, plain graphs in Light, and effects in Dark.
They rendered with zero warnings and passed visual inspection. Capture command (run from this branch):

`dotnet tools/Stats.UiPreview/bin/Debug/net8.0-windows/Stats.UiPreview.dll --batch C:/claude-projects/Stats/artifacts/pr15-merge/captures.json`

Images/sidecars live under `C:/claude-projects/Stats/artifacts/pr15-merge/`; recapture after committing the merge
ensures sidecar SourceCommit identifies the resolved tree, since this harness does not hash staged changes.
Previously documented native/hardware limits still apply.
