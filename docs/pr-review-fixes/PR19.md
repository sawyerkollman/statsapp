# PR #19 review repairs

Branch: `feature/overlay-sparklines`. Reviewed original head: `cc115ce`.
Ported from the independently reviewed integration repair `c42264b`; no other PR feature is merged here.

Give empty-overlay status a bounded width; validate stored overlay graph enums. Include the shared chart cache repair.

## Verification

- `dotnet build Stats.sln --nologo -v:q`: passed, 0 warnings, 0 errors.
- `dotnet test Stats.sln --nologo --no-build --no-restore -v:q`: 824 Core + 212 preview tests passed, 0 failed/skipped.
- 4 isolated WPF RenderTargetBitmap captures completed with 0 warnings using
  `dotnet tools/Stats.UiPreview/bin/Debug/net8.0-windows/Stats.UiPreview.dll --batch C:/claude-projects/Stats/artifacts/pr-fixes-branches/captures-19.json`.
- Local images: `C:/claude-projects/Stats/artifacts/pr-fixes-branches/evidence/pr19-*.png`.
- Independent Sol review found no material port/regression issue. Captures use simulated data at 96 DPI.
- Independent Sol visual validation inspected the actual PNGs and passed the scoped scenarios. Dashboard 1180x720 capture canvases include 39 transparent bottom rows; they are not 720px client-area or 150%-DPI checks.
- New test files are included in this commit. WPF test-project enablement retains the System.IO global using; no package changes.

Terra performed the bounded source/test port; the parent owned project files and validation, with independent Sol review/visual validation. Explicit role routing reused the successful session preflight; effective model metadata is unobservable.

Native routed keyboard/menu-close focus, physical drag feel, live theme transitions, actual 150% DPI, real overlay/hotkeys/tray,
PresentMon, and hardware/fan checks remain pending. Tests call rendering/cache helpers directly, not the full native interaction path.

The empty-overlay fallback intentionally supersedes the original spec's assumption that MaxWidth=0 harmlessly hides status: actual WPF produced a blank 24x1190 strip; the repair produces readable 246x60 status while preserving populated/off paths.
