# Stats v1.7 "Trust & polish" Implementation Plan

> Implement task-by-task on `feature/v1.7-trust-polish`. Read the linked design and
> the relevant existing subsystem spec before editing. Check boxes are execution
> state, not aspirational scope.

**Goal:** Deliver Batch 1 from the 2026-08-27 owner work order with crash-safe fan
cleanup, persistent diagnostics, runtime sensor health, dark control templates,
minimized/autostart behavior, About/update polish, SHA-256 verification, targeted
correctness fixes, hygiene, tests, README updates, and a pushed PR.

**Architecture:** Add testable state and parsing to `Stats.Core`; keep WPF,
`Process`, Task Scheduler, trace-listener registration, and shell launching in
`Stats.App`. Preserve the existing composition root, poller thread, settings gate,
and clean-exit paths.

**Spec:** `docs/superpowers/specs/2026-08-27-v1.7-trust-polish-design.md`

## Global gates

- [x] Baseline `dotnet build --nologo`: 0 warnings, 0 errors.
- [x] Baseline `dotnet test --nologo`: 400 passed, 0 warnings.
- [x] Create `feature/v1.7-trust-polish` from `master@18c2b92`.
- [x] After every task, run the smallest relevant focused tests, then full build and
  test before committing.
- [x] Never launch the elevated app or PresentMon from this shell.
- [x] Do not modify or commit the owner handoff file.

## Task 1: Enforce warnings and cover existing blind spots

**Files:**

- Create: `Directory.Build.props`
- Modify: `src/Stats.Core/Sensors/PerfCounterSensorReader.cs`
- Test: `tests/Stats.Core.Tests/FileFanArmedMarkerTests.cs`
- Test: `tests/Stats.Core.Tests/PerfCounterSensorReaderTests.cs`
- Test: `tests/Stats.Core.Tests/UpdateServiceTests.cs`
- Test: `tests/Stats.Core.Tests/SensorPollerTests.cs`

- [ ] Add `TreatWarningsAsErrors=true`; build immediately and resolve only warnings
  caused or exposed by this batch.
- [ ] Add `PerfCounterSensorReader.Discover()` re-entry guard matching LHM behavior.
- [ ] Test marker set/detect/clear and unusable-path `Set()` failure.
- [ ] Test repeat perf-counter discovery without asserting hardware-specific metrics.
- [ ] Test update download success, progress completion, and cancellation.
- [ ] Test live poll-interval changes.
- [ ] Run focused tests, full build, and full tests.
- [ ] Commit with `build:` / `fix(core):` / `test(core):` prefixes as appropriate.

## Task 2: Persistent diagnostics and fatal fan cleanup

**Files:**

- Create: `src/Stats.App/Helpers/RollingTraceLog.cs`
- Modify: `src/Stats.App/App.xaml.cs`
- Modify: `src/Stats.Core/ViewModels/SettingsViewModel.cs`
- Modify: `src/Stats.App/Views/DashboardWindow.xaml`
- Test a pure pruning helper if practical without coupling Core to WPF.

- [ ] Install rolling listener as the first `OnStartup` action; enable `AutoFlush`.
- [ ] Prune `stats-*.log` to seven newest files.
- [ ] Register dispatcher, AppDomain, and unobserved-task exception handlers.
- [ ] Use a single interlocked fatal-cleanup guard.
- [ ] Trace fatal details, best-effort restore fans, flush logs, and allow termination.
- [ ] Log-only for unobserved tasks.
- [ ] Add Diagnostics section and Open log folder action with inline failure state.
- [ ] Preserve normal `OnExit` ordering and do not introduce parallel LHM access.
- [ ] Run build and tests.
- [ ] Commit `feat(app): add crash safety diagnostics`.

## Task 3: Runtime sensor health

**Files:**

- Modify: `src/Stats.Core/Sensors/SensorPoller.cs`
- Modify: `src/Stats.Core/Sensors/CompositeSensorReader.cs`
- Modify: `src/Stats.Core/ViewModels/DashboardViewModel.cs`
- Modify: `src/Stats.App/App.xaml.cs`
- Modify: `src/Stats.App/Views/DashboardWindow.xaml`
- Test: `tests/Stats.Core.Tests/SensorPollerTests.cs`
- Test: `tests/Stats.Core.Tests/CompositeSensorReaderTests.cs`
- Test dashboard state in the existing ViewModel test file.

- [ ] Add immutable health/failure state and a poll-thread health-change event.
- [ ] Surface composite child name plus first error line while preserving partial data.
- [ ] Count unhealthy reads, keep the episode start time stable, and reset on a fully
  healthy read.
- [ ] Dispatch UI changes and show only at three or more consecutive failures.
- [ ] Clear the banner on recovery without clearing startup degraded status.
- [ ] Test full failures, partial backend failures, threshold, recovery, and subscriber
  isolation.
- [ ] Run focused sensor tests, full build, and full tests.
- [ ] Commit `feat(core): surface runtime sensor failures` and app wiring as needed.

## Task 4: Correctness and persistence

**Files:**

- Modify: `src/Stats.App/Views/DashboardWindow.xaml`
- Modify: `src/Stats.Core/ViewModels/MetricTileViewModel.cs`
- Modify: `src/Stats.App/App.xaml.cs`
- Test the history-tag calculation in the existing ViewModel tests.

- [ ] Add `Delay=250` only to poll interval and overlay font scale.
- [ ] Compute history tag from actual capacity and current interval, with compact
  seconds/minutes formatting consistent with existing labels.
- [ ] Add one shared approximately-five-second dispatcher debounce for Dashboard,
  Peaks, and Fans bounds persistence.
- [ ] Keep explicit final bounds/settings save in `ExitApp`/`OnExit`.
- [ ] Test the 60-minute/0.5-second clamp displaying 30 minutes.
- [ ] Run build and tests.
- [ ] Commit `fix(app): debounce settings persistence` and/or `fix(core): report
  effective history window`.

## Task 5: Minimized startup and Task Scheduler integration

**Files:**

- Create: `src/Stats.App/Helpers/StartupTaskService.cs`
- Modify: `src/Stats.App/App.xaml.cs`
- Modify: `src/Stats.Core/ViewModels/SettingsViewModel.cs`
- Modify: `src/Stats.App/Views/DashboardWindow.xaml`
- Modify: `installer/Stats.iss`

- [ ] Parse `--minimized` case-insensitively and skip only initial dashboard `Show()`.
- [ ] Refresh dashboard once through `ShowDashboard()` on the first tray show.
- [ ] Append `--minimized` to installer Scheduled Task `/TR`.
- [ ] Implement async `/Query`, `/Create`, and `/Delete` for task `Stats`, capturing
  exit code and stderr.
- [ ] Use current executable plus the exact installer task parameters.
- [ ] Add Startup section whose checkbox reflects actual task state, not settings.
- [ ] Disable or mark busy during mutation; re-query after completion; show failures.
- [ ] Run build and tests.
- [ ] Commit `feat(app): add minimized autostart`.

## Task 6: About, release link, and restart

**Files:**

- Modify: `src/Stats.Core/ViewModels/DashboardViewModel.cs`
- Modify: `src/Stats.Core/ViewModels/SettingsViewModel.cs`
- Modify: `src/Stats.App/Views/DashboardWindow.xaml`
- Modify: `src/Stats.App/App.xaml.cs`
- Extend existing updater/ViewModel tests where logic is testable.

- [ ] Expose formatted entry-assembly version and development-build state.
- [ ] Add manual check request, busy state, and inline result.
- [ ] Reuse `UpdateService.CheckAsync`; keep automatic failures silent but manual
  failures explicit.
- [ ] Feed a found update into the existing banner.
- [ ] Bind and open `UpdateInfo.ReleasePageUrl` as **What's new**.
- [ ] Add Hardware **Restart now**; start current exe and then call `ExitApp()`.
- [ ] Run build and tests.
- [ ] Commit `feat(app): polish update and restart flows`.

## Task 7: SHA-256 release integrity

**Files:**

- Modify: `.github/workflows/release.yml`
- Modify: `src/Stats.Core/Updates/UpdateChecker.cs`
- Modify: `src/Stats.Core/Updates/UpdateService.cs`
- Modify: `tests/Stats.Core.Tests/UpdateCheckerTests.cs`
- Modify: `tests/Stats.Core.Tests/UpdateServiceTests.cs`

- [ ] Add nullable `Sha256` to `UpdateInfo`.
- [ ] Parse only a valid 64-hex `SHA256:` release-body line; normalize case.
- [ ] Preserve old release behavior when absent.
- [ ] Keep size verification and add hash verification after download.
- [ ] Delete the destination on size/hash mismatch.
- [ ] Compute installer SHA in CI and build the release body with the hash line.
- [ ] Test present, absent, malformed, verified, and mismatch cases.
- [ ] Run focused updater tests, full build, and full tests.
- [ ] Commit `fix(core): verify update installer sha256` and
  `build: publish installer sha256`.

## Task 8: Palette-driven control templates

**Files:**

- Create: `src/Stats.App/Views/Controls.xaml`
- Modify: `src/Stats.App/App.xaml`
- Modify: `src/Stats.App/Views/DashboardWindow.xaml.cs`

- [ ] Merge Controls after Theme and before tile templates.
- [ ] Implement ScrollBar, Slider, ProgressBar, ComboBox/ComboBoxItem,
  ContextMenu/MenuItem, CheckBox, RadioButton, and Expander templates.
- [ ] Use `DynamicResource` for every palette brush.
- [ ] Cover focus, hover, pressed, checked, selected, disabled, and popup states.
- [ ] Keep existing Button and TextBox styles.
- [ ] Remove hard-coded ComboBox item foreground and submenu text pin.
- [ ] Build after each template family if XAML diagnostics require incremental work.
- [ ] Run full build and tests.
- [ ] Commit `feat(app): add themed control templates`.

## Task 9: README, hygiene, review, and delivery

**Files:**

- Modify: `README.md`
- Delete: `hello.txt`
- Update this plan's check boxes.

- [x] Document every user-visible Batch 1 behavior.
- [x] Confirm no owner-captured PresentMon fixture exists; if absent, defer without
  creating one.
- [x] Delete `hello.txt`.
- [x] Safely delete the four merged local branches with `git branch -d`.
- [x] Commit repository hygiene separately.
- [x] Run `dotnet build --nologo` and confirm 0 warnings.
- [x] Run `dotnet test --nologo` and confirm all green with 0 warnings.
- [x] Review the complete diff for settings compatibility, fan cleanup reentrancy,
  startup interactions, hard-coded template colors, and update-file cleanup.
- [x] Run an independent whole-branch code review and fix all high-confidence issues.
- [x] Re-run both gates after the fix wave.
- [ ] Push `feature/v1.7-trust-polish`.
- [ ] Open a PR to `master` with the owner verification checklist from the design,
  the license decision request, and real PresentMon fixture deferral if applicable.

## Stop condition before Batch 2

Do not create `feature/v1.8-monitoring-depth` until the v1.7 PR is merged to
`master`. Batch 2 must branch from that merged result.
