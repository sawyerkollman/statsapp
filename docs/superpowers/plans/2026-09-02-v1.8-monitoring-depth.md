# Stats v1.8 "Monitoring depth" Implementation Plan

> Implement task-by-task on `feature/v1.8-monitoring-depth`. Read the linked design and
> the relevant existing subsystem spec before editing. Check boxes are execution state.

**Goal:** Deliver Batch 2 from the 2026-08-27 owner work order: alerts, a rule-driven
threshold system, a tile detail chart with honest gaps, peak timestamps/copy, a tray
metric picker, overlay move mode, dashboard interaction polish, FPS discoverability,
Fans window UX, refresh-path efficiency, and an accessibility floor.

**Architecture:** New testable logic in `Stats.Core` (`Alerts/`, `Metrics/`,
`ViewModels/`, `Fans/`); WPF surfaces in `Stats.App`. Composition stays in
`App.xaml.cs`. Settings additive with defaults; migrations in
`ThresholdDefaults.EnsureDefaults`.

**Spec:** `docs/superpowers/specs/2026-09-02-v1.8-monitoring-depth-design.md`

## Global gates

- [x] `master` fast-forwarded to PR #9 merge; post-merge autofix regressions repaired
  (`8ec43e2`); baseline build 0 warnings, 500 tests green.
- [ ] After every task: focused tests, then `dotnet build --nologo` and
  `dotnet test --nologo` with zero warnings, then commit.
- [ ] Never launch the elevated app or PresentMon from this shell.
- [ ] Do not modify or commit the owner handoff file.

## Task 1: History gaps, detail view, peak timestamps (§3, §4)

- Modify: `MetricHistory.cs`, `MetricStore.cs`, `MetricTileViewModel.cs`,
  `PeaksViewModel.cs`, `Controls/Sparkline.cs`, `PeaksWindow.xaml(.cs)`,
  `DashboardWindow.xaml.cs`, `App.xaml.cs`
- Create: `ViewModels/MetricDetailViewModel.cs`, `Controls/HistoryChart.cs`,
  `Views/MetricDetailWindow.xaml(.cs)`
- Tests: `MetricHistoryTests.cs` (rewrite gap cases), `PeaksViewModelTests.cs`,
  new `MetricDetailViewModelTests.cs`

- [ ] NaN sentinel advance; min/max/avg ignore NaN; `SessionMinAtUtc/MaxAtUtc`.
- [ ] Sparkline finite-range + gap segments.
- [ ] Detail VM (labels, guides, stats, time axis) + chart + window; double-click and
  Details… entry points; refresh while visible.
- [ ] Peaks timestamps subtext/tooltip; `ToTsv`; Copy button with inline error.
- [ ] Commit `feat(core): honest history gaps and peak timestamps`,
  `feat(app): add metric detail window`.

## Task 2: Alerts (§1)

- Create: `Core/Alerts/AlertEngine.cs`, `AlertEvent.cs`, `AlertSample.cs`,
  `ViewModels/AlertLogViewModel.cs`; tests `AlertEngineTests.cs`, `AlertLogViewModelTests.cs`
- Modify: `AppSettings.cs`, `SettingsViewModel.cs`, `PeaksWindow.xaml` (tabs),
  `DashboardWindow.xaml` (Alerts section), `App.xaml.cs` (tick, balloon, sound)

- [ ] Engine with hold/re-arm/flapping/peak/duration; settings with defaults.
- [ ] Tray `ShowNotification`, `SystemSounds`, alert log tab capped at 200.
- [ ] Commit `feat(core): add alert engine`, `feat(app): surface alerts`.

## Task 3: Threshold rework (§2)

- Modify: `ThresholdDefaults.cs`, `SettingsViewModel.cs`, `DashboardViewModel.cs`,
  `DashboardWindow.xaml(.cs)`
- Create: `Core/Metrics/ThresholdInput.cs`, `ViewModels/ThresholdRuleItemViewModel.cs`,
  `Views/ThresholdDialog.xaml(.cs)`
- Tests: `ThresholdEvaluatorTests.cs`, `SettingsViewModelTests.cs`, `ThresholdInputTests.cs`

- [ ] Motherboard default + migration; rule-driven grid with Add rule…; per-row
  validation.
- [ ] `ThresholdDialog` with explicit direction; `SetTileThresholds(rule)`.
- [ ] Commit `feat(core): rule-driven thresholds`, `feat(app): threshold dialog`.

## Task 4: Tray picker, overlay move mode, FPS hint (§5, §6, §8)

- [ ] `TrayMetricId` + `TrayMetricSelector` + Settings combo + App fallback.
- [ ] Move mode: tray item, `IsMoveMode` outline, Esc/toggle exit, click-through restore.
- [ ] `FpsHintDismissed`, hint banner, Game caption in picker.
- [ ] Commit `feat(app): tray metric picker and overlay move mode`, `feat(app): fps hint`.

## Task 5: Dashboard polish and accessibility (§7, §11)

- [ ] `[RelayCommand]` promotion with parameter records; tests.
- [ ] DragOver group check + `InsertionAdorner`; hover ⋯ button; focusable tiles +
  Shift+F10/Apps key; single menu builder.
- [ ] `DashboardUiScale` + slider + LayoutTransform; `AutomationLabel`; `SeverityGlyph`.
- [ ] Commit `feat(app): dashboard interaction polish`, `feat(app): accessibility floor`.

## Task 6: Fans window UX (§9)

- [ ] Dropdown follows `ActiveProfile`; `LastLoadedProfile`, `IsModified`, Reload.
- [ ] `FanController.Identify` + Tick handling + tests (Auto and Manual restore).
- [ ] `FanSafetyBannerCollapsed` + Got it.
- [ ] Commit `feat(core): fan identify pulse`, `feat(app): fans profile follow and banner`.

## Task 7: Refresh-path efficiency (§10)

- [ ] Three-phase `Tick` with hardware calls outside `_gate`; `FanControllerTests` green.
- [ ] `MetricHistory.CopyTo` + tile double-buffer; tests for reuse/zero-alloc semantics.
- [ ] Visibility gating + refresh on Show; interlocked coalescing with latest snapshot.
- [ ] `ThresholdIndex`; picker `CurrentText` change check.
- [ ] Commit `perf(core): move fan writes outside settings gate`,
  `perf(app): coalesce and gate refreshes`.

## Task 8: Docs, review, delivery

- [ ] README for every user-visible change.
- [ ] Independent whole-branch review; fix high-confidence findings; re-run gates.
- [ ] Push; PR to `master` with the owner checklist from the design.
