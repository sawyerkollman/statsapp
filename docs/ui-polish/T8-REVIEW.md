# T8 — independent review and validation

Reviewer/validator: Claude Opus (independent of every implementer). Read-only review — no tracked file was edited.

- Candidate: branch `ui-polish` @ `d57fe67` ("feat(app): peaks, detail, overlay, and dialog consistency (T7)")
- Base: `master` @ `ad5ec1d` (v1.8.0)
- Tracked tree clean at review time; untracked only (`AGENTS.md`, `.codex/`, `HANDOFF.md`, `Stats-UI-Codex-Handoff/`)
- Diff reviewed in full: `git diff master...ui-polish -- . ':!docs' ':!artifacts'` → 56 files, +4307 / −707
- Evidence generated by me: `artifacts/ui-polish/after.json` (56 entries) → 61 PNG + 61 JSON sidecars in
  `artifacts/ui-polish/after/`, plus 4 ad-hoc re-captures (`retry-tile-menu`, `v11b-settings-invalid-*`).
  **All 61 sidecars report `"Warnings": []`** (binding + resource trace listeners at `SourceLevels.Error`).

Bottom line: the shared-resource work (T2), binding/behavior preservation (T4/T5), and fan-safety posture (T6)
hold up under inspection. Three rendered-output defects — two of them regressions against `master` — and one
unexplained density drop block a `visually accepted` call.

---

## 1. Findings

Ranked by severity. "Evidence" cites a capture under `artifacts/ui-polish/` or a diff location.

### Blockers

| # | File:line | What is wrong | Evidence | Minimal fix |
|---|---|---|---|---|
| B1 | `src/Stats.App/Views/CoreMatrixView.xaml:16` (`Border Width="60" Height="54"`), with `:19` / `:23` / `:25` raised from `FontSize="9"` to `FontSizeDense` (11) | The per-core cell's four text rows (index 11 + load 13 + clock 11 + temp 11) need ~62 units; the cell is 54. **The per-core temperature line is clipped on every core that reports one**, and the `#n` index is clipped at the top. This is a regression: at 56×50 with 9pt metadata all four rows fitted cleanly. DESIGN §4 requires core cells to "keep meaningful load, clock, and temperature information". | `after/v2-dashboard-dense.png` vs `before/v2-dashboard-dense.png` (core grid, y≈185–300); worse at `after/v5-dashboard-dense-scale09.png` | Raise the cell to `Height="64"` (and `Width="64"`), or drop index/clock/temp back to 10. One attribute. |
| B2 | `src/Stats.App/Views/PeaksWindow.xaml:39-43` (Peaks header) vs `:57-61` (Peaks row), `:106-110` (Alerts header) vs `:124-128` (Alerts row) — `Width="*" MinWidth="140"` Metric column inside a `HorizontalScrollBarVisibility="Auto"` `ScrollViewer`, with header and each row as **separate** `Grid`s | Two failures at once. (a) A horizontally-scrolling `ScrollViewer` measures its content at infinite width, so the `*` Metric column degenerates to `Auto` and `TextTrimming="CharacterEllipsis"` never fires — long names push the table wide and Avg/Max fall off-screen. (b) Because the header `Grid` has no long name and each row `Grid` sizes independently, **header columns no longer line up with row columns**. Both are regressions: `master`'s fixed 100-unit numeric columns ellipsized the name and stayed aligned. Directly violates VALIDATION's "column headers aligned with rows" and "numeric columns … remain stable". | `after/v10-peaks-long-names.png` (only Now + half of Min visible; "Now"/"Min" headers offset ~28 units from their values) vs `before/v10-peaks-long-names.png` (all 5 columns, aligned, names ellipsized) | Give the five columns a `SharedSizeGroup` each (`Grid.IsSharedSizeScope="True"` on the common `DockPanel`) and keep the Metric column `*` with the outer `ScrollViewer`'s `HorizontalScrollBarVisibility="Disabled"` above a threshold — or simply restore fixed numeric widths and keep only the 480-wide horizontal scroll. The 480×240 case (`after/v10-peaks-populated-narrow.png`) already works and should be preserved. |

### Should-fix

| # | File:line | What is wrong | Evidence | Minimal fix |
|---|---|---|---|---|
| S1 | Aggregate of `TileSizeToLengthConverter.cs:16-18` (M 215×120 → 224×144), `TileTemplates.xaml:17` padding 10→12, `DashboardWindow.xaml:167` `Margin="0,24,0,0"`, `:164` inset 16,16,16,16, taller header | **V2 fully-visible medium tiles fell 3 → 1 (−67 %); total fully-visible tiles 8 → 4 (−50 %).** DESIGN §8 requires any reduction over 20 % to be explained and recorded with before/after evidence. No such record exists — T3-REPORT's "8 … no reduction" was measured on the pre-T4 tree and is stale for the integrated candidate. | §3 below; `before/v2-dashboard-dense.png` vs `after/v2-dashboard-dense.png` | Either record the orchestrator decision in `EVIDENCE.md` with the two PNGs, or recover height (section margin 24→16, tile M height 144→132). V1 (8 → 7, −12.5 %) needs no action. |
| S2 | `src/Stats.App/Views/FansWindow.xaml:63–140` (header + profile row + safety banner + the new "Game mode" section header and hint line, all `DockPanel.Dock="Top"`) | At the window's own documented minimum (560×360) **no fan card is visible at all** — the fixed top chrome consumes the whole client area and the card `ScrollViewer` is ~20 units tall. `master` showed one complete fan card at the same size. | `after/v9-fans-on-narrow.png` vs `before/v9-fans-on-narrow.png` | Collapse the Game-mode section into an `Expander` (or drop the second hint line and the section heading) when short; or raise `MinHeight` deliberately and record it — DESIGN §3's rule against silently raising a minimum to hide a layout defect applies here too. |
| S3 | `src/Stats.App/Views/FansWindow.xaml:199` — `<StackPanel … IsEnabled="{Binding ControlEnabled}">` around the three mode RadioButtons | New behavior: with the master switch **off**, a channel's Auto/Manual/Curve mode can no longer be changed. On `master` the radios were always enabled (`ControlEnabled` gated only `IdentifyCommand`; see `FansViewModel.cs:76-77`), so "configure the channels, then arm" was a supported flow. It is also internally inconsistent — the Manual slider and the curve editor below stay enabled while the mode selector is greyed. | `after/v9-fans-off.png` (segmented control at 0.5 opacity) vs `before/v9-fans-on-narrow.png`/master XAML | Drop the `IsEnabled` binding (mode changes write only desired state; `FanController.Enabled=false` performs no hardware write), or extend the same gate to the Manual/Curve editors so the disabled state is coherent. |
| S4 | `tools/Stats.UiPreview/captures/baseline.json:47-49` (the three `View: "settings"` invalid-state entries) | The gallery manifest was never updated for T5's nested category tabs. Re-running it now captures the **Appearance** category, so `v11-settings-invalid-threshold/-hotkey/-limit` show none of the states they are named for — and the run still reports "0 warnings, 56/56 captured". Silent evidence loss. | `after/v11-settings-invalid-threshold.png` (Appearance tab, no threshold grid) vs my re-capture `after/v11b-settings-invalid-threshold.png` (correct, red border + "Warn must be a number") | Append `+category-monitoring` / `+category-overlay` to those three manifest entries. |
| S5 | `src/Stats.Core/ViewModels/FansViewModel.cs:379` `[RelayCommand] private void DeleteProfile(string name)`; consumed by `FansWindow.xaml.cs:38-56` | `DeleteProfileCommand` has **no `CanExecute`**, so the generated `RelayCommand<string>.CanExecute(null)` returns `true`. T6-REPORT states "the generated command already refuses a null parameter, so the item disables itself when nothing is selected" — that is false, and the `[NotifyCanExecuteChangedFor(nameof(DeleteProfileCommand))]` at `:339` (plus its "the button greys out" comment) is inert. Pre-existing on `master`, but T6 now relies on it. Clicking `Delete` with no selection calls `RemoveProfile(null)`, which is a harmless no-op, so the impact is a misleading enabled menu item, not data loss. | Source read; `after/v9-fans-on.png` (profile ComboBox empty, "…" menu still offers Delete) | Add `CanExecute = nameof(CanDeleteProfile)` with `private bool CanDeleteProfile(string? n) => !string.IsNullOrWhiteSpace(n);`, or have `ProfileMenu_Click` set `delete.IsEnabled = name is not null`. Correct the report either way. |
| S6 | Harness: `tools/Stats.UiPreview/Views/CaptureHost.cs:244-264` + `Screenshot.CaptureScreen` | Popup substates are flaky in batch mode. The full-batch `v6-tile-context-menu` captured **no menu**; an identical single-shot re-run captured it correctly. A batch that reports "0 warnings, all captured" can therefore silently drop the only evidence for a popup case. | `after/v6-tile-context-menu.png` (no menu) vs `after/retry-tile-menu.png` (full menu, all 7 items) | Assert after opening (e.g. `PresentationSource.CurrentSources` / an open `ContextMenu` in the popup tree) and fail the entry instead of capturing a blank frame. |

### Nits (code quality — "lazy senior" lens; none of these are correctness)

| # | File:line | What to cut / change |
|---|---|---|
| N1 | `src/Stats.App/Views/Icons.xaml:43` | `Icon.Crit` has **no consumer** (T3 kept the view-model's `‼`/`▲` text glyphs). Delete it, or use it where `Icon.Warn` currently doubles for critical. |
| N2 | `src/Stats.App/Views/Controls.xaml:734-737` (`HeaderButtonTemplate` hover trigger) + `:772-775` (`PrimaryButton`) | Hover sets `Bd.BorderBrush` to `BorderDim`, which **overwrites** `PrimaryButton`'s always-on `AccentBrush` border and `OverlayHeaderButton`'s on-state accent bar (`DashboardWindow.xaml:14-24`) — hovering the primary action or the "overlay is on" button makes it look less prominent / off. Guard the hover trigger with a `MultiTrigger` on `IsKeyboardFocused=False` + a `Tag`, or set hover on `Background` only. |
| N3 | `src/Stats.App/Helpers/ThemeManager.cs:128` | `PaletteFor` is `public` on a production class purely for `PaletteContrastTests`. `internal` suffices — `Stats.App.csproj` already grants `InternalsVisibleTo("Stats.UiPreview.Tests")`. |
| N4 | `src/Stats.App/Views/TileTemplates.xaml:103-110` | `TileMenuButtonStyle` sets both `Content="Tile options"` and a `ContentTemplate` that ignores the content. Keep the `ContentTemplate` + `AutomationProperties.Name`; drop the `Content` setter. |
| N5 | `src/Stats.App/Views/TileTemplates.xaml:57` | `TileOptionalText` attaches `ToolTip="{Binding Text, RelativeSource=Self}"` to **every** footer, including short ones that are not truncated — a redundant tooltip on most tiles. |
| N6 | `tools/Stats.UiPreview/Views/CaptureHost.cs` (`SettingsCategoryOrder`) | A hand-maintained array duplicating the XAML tab order. Reordering the five `TabItem`s silently captures the wrong category. Read the headers off `SettingsCategoryTabs.Items` instead. |
| N7 | `src/Stats.App/Views/DashboardWindow.xaml:300-303`, `:349-351` | The picker's column headers and rows still hardcode `FontSize="11"` / `"12"` while everything else in the same file moved to `FontSizeDense` / `FontSizeLabel`. |
| N8 | `src/Stats.App/Views/DashboardWindow.xaml:258` | `PickerFilterBox` has no `AutomationProperties.Name`; its label is a sibling placeholder `TextBlock`, invisible to AT once the user types. One attribute. |
| N9 | `src/Stats.App/Views/MetricDetailWindow.xaml:11` | Severity is conveyed by **colour only** (no glyph) in the detail header — DESIGN §2 asks for a glyph alongside colour. Pre-existing on `master`, but T7 owned this file and touched that exact line. |
| N10 | `src/Stats.App/Views/Theme.xaml:62` vs `Helpers/ThemeManager.cs:46` | `OverlayCrit` stays `#FFE05A4F` while `DarkCrit` moved to `#FFE66E64` — the dashboard and overlay critical colours now differ. Deliberate under "overlay colours pinned", but undocumented. (The overlay value text is 16pt SemiBold, so the 4.18:1 it retains still meets the 3:1 large-text target.) |
| N11 | `src/Stats.App/Views/FansWindow.xaml:187` | The channel-name `TextBox` went from a `*` Grid column to a fixed `Width="150"`; long channel names now scroll inside a small box instead of using available width. |
| N12 | `src/Stats.App/Views/FansWindow.xaml:116` | Safety copy dropped "Changes apply on the next sensor poll (≈1 s)". The two facts DESIGN §6 protects are kept; this is a small loss of user-visible timing information. |
| N13 | `src/Stats.Core/Metrics/ValueFormatter.cs:24` | `unit.Length == 0 ? val : $"{val} {unit}"` replaced `.TrimEnd()`. A whitespace-only `Unit` would now leave a trailing space. No current `MetricDefinition` has one; flagged only because `Format`'s contract changed shape. |
| N14 | `src/Stats.App/Views/Controls.xaml:725-753` | `HeaderButtonTemplate` is a separate keyed `ControlTemplate` with exactly one consumer (`HeaderButton`). Inline it into the style unless a second consumer is coming. |
| N15 | `src/Stats.App/Views/AppStyles.xaml:9-10` | Stray double blank line left by the extraction. |

---

## 2. Verified clean

Each of these was checked mechanically, not by reading the implementers' reports.

**Theme / live switching**
- `grep` for `StaticResource` against all eleven palette keys (`WindowBg`, `TileBg`, `FlyoutBg`, `ControlBg`,
  `BorderDim`, `TextPrimary`, `TextSecondary`, `AccentBrush`, `WarnBrush`, `CritBrush`, `GaugeTrack`) across
  `src/` and `tools/` → **zero hits**. Every palette reference is `DynamicResource`.
- Merge order is identical and explicit in both hosts: `App.xaml:10-24` and
  `tools/Stats.UiPreview/PreviewApp.cs:21-25` → Theme → Icons → Controls → TileTemplates → AppStyles.
- Double-merge rule honoured: the typography/shape tokens live in `Theme.xaml:64-79` (`FontSizeTitle` at `:68` … `ControlMinHeightDense` at `:79`) (the dictionary
  `TileTemplates.xaml` re-merges). `TileTemplates.xaml:107`'s `Icon.More`/`IconGlyph` resolve through the
  established pattern — dictionaries merged *before* it at app level are visible (the same mechanism
  `master` already relied on for `HeaderButton`), and Icons/Controls are both merged earlier in both hosts.
- Overlay pinned brushes (`OverlayTextPrimary/Secondary`, `OverlayWarn`, `OverlayCrit`) unchanged.
- Exactly **three** colour-value changes in the whole diff (`ThemeManager.cs`: shared `DarkCrit`, Light
  `AccentBrush`, Light `CritBrush`). Every other hex literal — including the pinned `#7A2D2D`, `#6B4E12`,
  `#4A3A1E`, `#4A1E1E`, `#5A1E1E` banner tints — is byte-identical; only padding/radius around them moved.
- Live repaint verified: `after/v6-theme-cycle-{0..5}` — Light and the custom teal accent repaint fully, no
  stale amber, essential text stays `TextPrimary`.

**Behavior preservation**
- Extracted every `Command=` / `Click=` / event-handler attribute from `master` and `ui-polish` for all nine
  changed views and diffed the multisets. Only intended deltas: `CollapseAllCommand`/`ExpandAllCommand` moved
  into `ViewButton_Click`'s code-behind menu, `DeleteProfileCommand`/`CreateDefaultProfilesCommand` into
  `ProfileMenu_Click`'s, plus the new `HeaderOpener_Click`/`PickerClose_Click`/`PickerFilterClear_Click`/
  `PickerFlyout_KeyDown`/`PickerFlyout_IsVisibleChanged`. `PeaksWindow`, `MetricDetailWindow`,
  `OverlayWindow`, `InputDialog`, `ThresholdDialog`, `TileTemplates`, `CoreMatrixView` are **identical**.
- Tile `ItemContainerStyle`: all nine `EventSetter`s byte-identical (`DashboardWindow.xaml:211-219`).
- Picker group All/None: `Tag="{Binding Name}"` + `PickerGroupAll_Click`/`PickerGroupNone_Click` unchanged
  (`:314`, `:316`).
- **T5 Settings parity confirmed independently**: extracting every binding/command/click-bearing attribute
  inside `<TabItem Header="Settings">` from both revisions and diffing the sorted multisets produced
  **no differences at all** — all 26 inventory rows present exactly once, nothing dropped or duplicated, and
  exactly one `DataContext="{Binding SettingsPanel}"` (`:378`), so there is still one `SettingsViewModel`.
- Fans: `SaveAs_Click`, `ReloadCommand`, `SetAllAutoCommand`, `SaveProfileCommand`,
  `CreateDefaultProfilesCommand`, `DeleteProfileCommand`, `DismissSafetyBannerCommand`,
  `DismissRecoveryNoticeCommand`, `ResetCurveCommand`, `IdentifyCommand` all still reachable.
- Peaks: `Copy_Click`, `ResetSessionCommand`, `ClearCommand`, `IncludeAll`, `CopyError`/`HasCopyError`,
  `MinAtText`/`MaxAtText` tooltips, alert order and `DurationText` "ongoing" all unchanged
  (`after/v10-alerts-ongoing.png` shows "ongoing").
- Dialogs: `IsDefault`/`IsCancel` on OK/Cancel preserved in both dialogs; `Ok_Click` validation path
  untouched; no `.xaml.cs` changes. `after/v11-threshold-dialog-invalid.png` shows the error row.
- `AllowClose` / `OnClosing` hide-to-tray path is untouched in the `DashboardWindow.xaml.cs` diff.
- **`App.xaml.cs` is exactly the two required one-liners** — `Settings = _settings` on the dashboard
  construction (`:229`) and `_overlay.IsVisibleChanged += … IsOverlayVisible = …` (`:264`). Nothing else.

**Core invariants**
- `Stats.Core` diff touches only `Metrics/ValueFormatter.cs` (`FormatParts` + `Format` recomposed on it),
  `ViewModels/MetricTileViewModel.cs` (`ValueText`/`UnitText`), `ViewModels/DashboardViewModel.cs`
  (`IsOverlayVisible`, `IsEmpty`, one `OnPropertyChanged` in `RebuildSections`) and tests. Nothing else.
- `MetricGroup`, `AppSettings`, `SettingsService` untouched; **no new persisted field** anywhere.
- `IsEmpty => Sections.Count == 0` is correct for the core-matrix case: `RebuildSections` creates the Cpu
  section when `CoreMatrix is not null` even with zero tiles (`DashboardViewModel.cs:391`, `:402`).
- No sensor/poller/fan-controller/threading change. `H.NotifyIcon.Wpf` still pinned at 2.3.2; no production
  dependency added or bumped (`System.Drawing.Common 10.0.11` in the harness matches `Stats.App`'s).

**Fan safety (CLAUDE.md rules 1 & 6)**
- `FansWindow.xaml` contains no `Command`/`Click` invoked from a template load, `Trigger`, `DataTrigger`,
  `EventTrigger`, or `Loaded`. Every command sits on a `Button` or a code-behind `MenuItem`.
- The three mode RadioButtons keep `GroupName="{Binding Id}"` (`:200`, `:202`, `:204`), so per-channel
  exclusivity and arrow-key navigation are preserved — confirmed visually in `after/v9-fans-curve.png`
  (Fan #1 on Auto, Fan #2 on Curve simultaneously).
- `Identify` and `All to Auto` are explicit user gestures only. `ProfileMenu_Click` builds its menu on demand
  and executes nothing at open time.

**Keyboard / accessibility**
- `Tile_PreviewKeyDown` (Apps / Shift+F10) is unchanged and still wired through the `ItemContainerStyle`;
  `OpenTileMenu` remains the single builder for right-click, the hover button, and the keyboard path. The
  re-captured menu (`after/retry-tile-menu.png`) has all 7 items intact.
- Flyout Escape logic is **sound**: `PickerFlyout_KeyDown` is a bubbling `KeyDown` (not `PreviewKeyDown`) with
  no `handledEventsToo`, so an open `ComboBox` dropdown — which marks Escape handled — never reaches it.
  `HotkeyBox_PreviewKeyDown` likewise consumes Escape first while capturing a hotkey.
- No null-ref and no focus trap: `_pickerOpener` is only ever `MetricsButton` or `SettingsButton`, both
  permanently loaded header children; `(_pickerOpener ?? MetricsButton)` cannot be null, and `Focus()` on a
  collapsed element returns `false` rather than throwing. Open-focus works — `after/v3-picker-narrow-scale13.png`
  shows the search box focused (accent border + caret) straight after the flyout opened.
- `AutomationProperties.Name` present on every icon-only control: flyout Close, clear-search, tile options,
  Fans "Profile options", "All fans to Auto". Decorative `Path`s are `IsHitTestVisible="False"`.
- Severity is never colour-only on tiles — the `▲`/`‼` glyph is retained (`after/v2-dashboard-dense.png`:
  "84.0 °C ▲", "100 % ‼"). See N9 for the detail window.

**Harness isolation (G2)**
- `InternalsVisibleTo` in `Stats.App.csproj` is limited to exactly `Stats.UiPreview` and
  `Stats.UiPreview.Tests`.
- Writes are confined to `%TEMP%\Stats.UiPreview\<run-id>\` (`Program.cs:58`, `PreviewComposition.cs:46`) plus
  the caller-requested `--output` PNG/sidecar paths. No `%AppData%\Stats` path anywhere.
- `IsolationMetadataTests` scans the built assembly's type/member-reference tables for the forbidden
  production types; `PaletteContrastTests` is a genuine WCAG 2.x luminance computation over
  `ThemeManager.PaletteFor`, not a tautology.

**The `fans no-channels` question — fixture only, not a product bug.** `FansViewModel.HasChannels` is
`_byId.Count > 0` (`FansViewModel.cs:231`); `_byId` is populated once in the constructor from
`controller.Views()` (`:210-216`) and the property raises no change notification. In production the window is
constructed after discovery, so the binding evaluates correctly at load. The harness clears the fake backend
*after* the view model snapshotted its channels, so the notice never appears. `before/v9-fans-no-channels.png`
shows the same three channels, so this is **pre-existing harness behavior, not a T6 regression** — but V9's
"no channels" case still has **no evidence** and must be fixed in `PreviewComposition` before it can be ticked.

---

## 3. Visible medium-tile counts (1180×720, Dark Amber, scale 1.0)

Counted by pixel-sampling the tile background boundary against the 713-unit client height.

| Case | Before | After | Change | DESIGN §8 |
|---|---|---|---|---|
| **V1 `normal`** — fully visible **M** tiles | **8** (Cpu ×4, Gpu ×3, Memory ×1; Storage tile top y=595, bottom 715 → clipped) | **7** (Cpu ×4, Gpu ×3; Memory tile top y=577, bottom 721 → clipped) | **−12.5 %** | under threshold, no explanation required |
| **V2 `dense`** — fully visible **M** tiles | **3** (Package Power (PPT), GPU Core, GPU Memory Controller Load) | **1** (Package Power (PPT) only) | **−67 %** | **over threshold — explanation required and missing (finding S1)** |
| **V2 `dense`** — fully visible tiles of any size | 8 (+ core matrix) | 4 (+ core matrix) | −50 % | same |

Drivers: M tile 215×120 → 224×144 (+20 % height), tile padding 10 → 12, per-section `Margin="0,24,0,0"`, a
16-unit content inset, a taller header row, and a core-matrix panel that grew from 56×50/10 padding to
60×54/12 padding. Legibility clearly improved (tokenised type, tabular figures, unit separated from value,
reserved menu column); the tradeoff is just not recorded anywhere.

---

## 4. Gate outcomes

| Gate | Result | Evidence |
|---|---|---|
| **G3 Build** | **pass** | `dotnet build --nologo` → exit 0, **0 Warning(s), 0 Error(s)**, 5 projects (Stats.Core, Stats.App, Stats.Core.Tests, Stats.UiPreview, Stats.UiPreview.Tests). Run by me on the candidate. |
| **G4 Tests** | **pass** | `dotnet test --nologo` → exit 0, **0 warnings**. `Stats.Core.Tests` 687 passed / 0 failed / 0 skipped. `Stats.UiPreview.Tests` 172 passed / 0 failed / 0 skipped. Total 859. Run by me. |
| **G5 Visual** | **fail** | 61 PNG + 61 sidecars generated on the candidate, **every `Warnings` array empty** — no binding or resource failures. But: B1 (core-matrix temperature clipped), B2 (Peaks long-name overflow + header/row misalignment), S2 (no fan card at the documented 560×360 minimum), S1 (V2 density −67 % unexplained). V4 (150 % actual Windows DPI) **not run** — no such display on this machine. V9 "no channels" has no evidence (fixture, see §2). |
| **G6 Interaction** | **blocked** | The harness produces static frames only. Partial static evidence exists (flyout open-focus, tile context menu re-capture, per-channel radio exclusivity, invalid-field red borders, empty states); everything in §5 remains unexercised. No step-by-step interaction log was produced by any task. |
| **G7 Compatibility** | **pass by inspection, live round-trip pending** | `AppSettings`/`SettingsService`/`MetricGroup` untouched, no new persisted field, serialized `TileSize`/`TileKind`/ids/order semantics unchanged (only the S/M/L → pixel mapping moved), Settings binding multiset byte-identical, live theme cycle verified in captures. VALIDATION interaction check 4 — load a real pre-change `settings.json`, navigate, restart, compare semantic values — was **not executed by anyone**. |
| **G8 Independent review** | **this document**; findings open | 2 blockers, 6 should-fix, 15 nits. None resolved yet. |

---

## 5. Pending — cannot be verified from static captures (owner checklist)

1. **V4: 150 % actual Windows DPI.** Both monitors here report 96 DPI. Command:
   `dotnet run --project tools/Stats.UiPreview -- --scenario normal --view dashboard --theme Light --width 1180 --height 720 --output artifacts/ui-polish/after/v4-dashboard-settings-light-150dpi.png` on a 150 %-scaled display.
2. **Keyboard.** Tab order through the wrapped header; Escape → focus returns to the invoking header button;
   focus placement when the flyout opens on the Settings tab; arrow/Ctrl+Tab across the five nested category
   tabs; a pending `LostFocus` threshold/limit edit committing when a category tab is clicked; Shift+F10 and
   the Apps key on a focused tile; Escape from the hotkey box (`Keyboard.ClearFocus()` leaves focus nowhere,
   so a second Escape will *not* close the flyout — confirm whether that matters).
3. **Hover / pressed states.** `HeaderButton`, `PrimaryButton` (see N2), the implicit `TextBox`,
   `SegmentedRadioButton`, `FanNameTextBox`, and the tile "…" button reveal-on-hover.
4. **Overlay on/off header indicator.** The harness never wires `IsVisibleChanged` between its independent
   Overlay and Dashboard compositions, so no capture shows `IsOverlayVisible == true`.
5. **Tray / windowing.** Close-to-tray (`AllowClose`), tray menu exit ordering, `--minimized` startup, the
   real global hotkey, overlay click-through, and overlay drag-to-move.
6. **Real hardware fans.** Actual writes, the Identify pulse, the three-failed-writes → Auto failsafe, the
   pump 50 % floor, `RestoreAll()` on exit. Simulated success cannot satisfy these (CLAUDE.md rules 1, 6).
7. **Peaks TSV clipboard** (`Copy_Click` success/failure) and **HistoryChart crosshair hover** (value + time).
8. **Settings-compatibility round-trip** with a real pre-v1.8 `settings.json` containing mixed tile sizes,
   custom labels/order, theme/accent, collapsed groups, threshold overrides, overlay options and fan profiles
   (VALIDATION interaction check 4).
9. **ToolTip chrome.** The new `Padding`/`BorderThickness` setters are unverified — no capture opens a live tooltip.
10. **Rendered contrast.** `PaletteContrastTests` measures palette hex values, not composited/anti-aliased
    output, and user-chosen custom accents are explicitly out of scope.
11. **PresentMon / FPS metrics** — cannot be exercised from this shell at all (CLAUDE.md rule 7).

---

## Fix wave

Fixed the two blockers, S2/S3/S4, and the four listed nits. Smallest-diff changes only; no colours, tokens,
view-model, or test changes. `dotnet build --nologo` → 0 Warning(s)/0 Error(s); `dotnet test --nologo` →
687 + 172 passed, 0 failed, 0 skipped. Captures in `artifacts/ui-polish/after-t8/` (`--method rtb` — the
`screen` capture method produces blank frames in this shell/session, so every after-t8 capture below used
`--method rtb` instead; all 11 sidecars report `"Warnings": []`).

| # | File(s) | Change | Proof |
|---|---|---|---|
| B1 | `src/Stats.App/Views/CoreMatrixView.xaml:16` | Cell `Height="54"` → `Height="64"` (kept `Width="60"` — the clock text did not clip at 60). | `after-t8/dense-dashboard-1180x720.png`: all four rows (`#n`, load, clock, temp) fully visible on every core cell. |
| B2 | `src/Stats.App/Views/PeaksWindow.xaml` (Peaks header/row Grids, Alerts header/row Grids) | On each tab's `ScrollViewer` content `DockPanel`: added `Grid.IsSharedSizeScope="True"`, bound `Width` to the ancestor `ScrollViewer`'s `ViewportWidth` with `MinWidth="428"` (sum of the five column minima), and gave each numeric column a `SharedSizeGroup` shared between the header Grid and the row template's Grid. | `after-t8/peaks-long-names-640x480.png`: all 5 columns visible, Metric ellipsized, headers aligned with values. `after-t8/peaks-populated-480x240.png`: horizontal scrollbar appears at the 428 floor, Metric stays readable. `after-t8/alerts-ongoing-640x480.png`: Alerts tab columns aligned, "ongoing" visible. |
| S2 | `src/Stats.App/Views/FansWindow.xaml` | Moved everything below the header row (profile row, safety/recovery/conflict/no-channels notices, Game mode section, fan-card `ItemsControl`) inside the existing `ScrollViewer`, as a single `StackPanel`; only the title row + master toggle + All to Auto stay pinned `DockPanel.Dock="Top"`. | `after-t8/fans-on-curve-560x360.png`: header pinned, profile/banner/Game-mode scroll, device heading + vertical scrollbar visible reaching toward the cards (previously nothing below the fixed chrome was visible at all). `after-t8/fans-on-curve-760x640.png`: unchanged full layout with both fan cards. |
| S3 | `src/Stats.App/Views/FansWindow.xaml:199` (mode `StackPanel`), `:8-13` (stale doc comment) | Removed `IsEnabled="{Binding ControlEnabled}"` from the Auto/Manual/Curve `StackPanel`; corrected the `SegmentedRadioButton` header comment (no longer claims a `ControlEnabled`-bound disabled state). | `after-t8/fans-off-760x640.png`: master switch off, all three mode radios full-opacity and selectable (not greyed). |
| S4 | `tools/Stats.UiPreview/captures/baseline.json` | Appended `+category-appearance` to `theme-dropdown` (line 14) and `+category-monitoring` / `+category-overlay` / `+category-monitoring` to `invalid-threshold` / `invalid-hotkey` / `invalid-limit` (lines 52-54). File re-validated as JSON. `restart-required`, `startup-error`, `update-error` are not present as manifest entries (only listed in `SubstateCatalog`/`PreviewComposition`'s allowed substates), so there was nothing stale to fix for those. | `after-t8/settings-invalid-limit-monitoring-1180x900.png`: Monitoring tab selected, "Package Power (W)" field shows `abc` with a red error border. `after-t8/settings-invalid-hotkey-overlay-1180x720.png`: Overlay tab selected, hotkey box shows `Q` with "Invalid hotkey" text. |
| N1 | `src/Stats.App/Views/Icons.xaml:43` | Deleted the unused `Icon.Crit` geometry (grepped `src/`, `tools/` — no XAML/code-behind consumer, only stale build artifacts referenced it). | Build 0 warnings/errors. |
| N2 | `src/Stats.App/Views/Controls.xaml` (`HeaderButtonTemplate`, `PrimaryButton`), `src/Stats.App/Views/DashboardWindow.xaml` (`OverlayHeaderButton`) | `Style.Triggers` cannot set `TargetName` on a template part directly (`MC4011` — confirmed by a failed build attempt), so instead: added a `MultiTrigger` (`IsMouseOver=True` + `Tag="AccentHover"`) to `HeaderButtonTemplate` that re-asserts `Bd.BorderBrush` = `AccentBrush`; `PrimaryButton` now sets `Tag="AccentHover"` unconditionally; `OverlayHeaderButton`'s existing `IsOverlayVisible=True` `DataTrigger` now also sets `Tag="AccentHover"` (so the accent-hover only applies in the "overlay is on" state). | Build 0 warnings/errors (XAML compiles); `after-t8/threshold-dialog-valid.png` shows the PrimaryButton's always-on accent border at rest (unaffected regression check). **Not directly captured**: the harness produces static frames only (per G6, "blocked" — no hover-state capture exists), so the hover-specific repaint itself was verified by reading the trigger precedence (`Style.Triggers` outrank `ControlTemplate.Triggers` for the same templated-parent target) rather than a screenshot. |
| N3c | `src/Stats.App/Views/DashboardWindow.xaml` (picker filter `TextBox`) | Added `AutomationProperties.Name="Search sensors"` to `PickerFilterBox`. | `after-t8/settings-invalid-limit-monitoring-1180x900.png` / `settings-invalid-hotkey-overlay-1180x720.png` show the flyout with the search box present (attribute is not visually renderable; confirmed by reading the XAML). |
| N3d | `docs/ui-polish/T6-REPORT.md` | Corrected the Delete-profile sentence: `DeleteProfileCommand` has no `CanExecute` (stays enabled with nothing selected); clicking it with nothing selected is a no-op, not a disabling behavior. | Doc-only; read back after edit. |

Not touched: S1 (density explanation — out of scope, no fix listed), S5/S6 and all nits not listed in the fix
set (N3, N4-N15) — left exactly as the review found them, per the "do not touch anything else" instruction.
